# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# --- Virtual environment ---
.venv/
venv/

# --- Vendored upstream repos (reproducible via setup.sh) ---
third_party/

# --- Secrets / local env ---
.env
.env.*
!.env.example
*.pgpass
.pgpass

# --- Models, caches, and other large/binary artifacts ---
*.pkl
*.pt
*.pth
*.onnx
dukascopy_cache/
**/dukascopy_cache/

# --- Runtime state, signals, logs (default state dir is ~/.trader-stack,
# outside the repo, but ignore defensively in case a run points inside it) ---
*.jsonl
*.log
*.pid
signals.jsonl
state.json
daemon.log
oracle.pid
replay_signals_*.jsonl
replay_checkpoint_*.json
regime_hmm*.pkl

# --- Postgres dumps / local DB state ---
*.sql
*.sql.gz
*.dump
pgdata/

# --- Claude Code local tool state (not shared/team config) ---
.claude/

# --- OS / editor cruft ---
.DS_Store
*.swp
.idea/
.vscode/

# --- Scratch / session artifacts (not part of the deliverable) ---
Terminal_data.txt

# --- Local Claude working instructions (internal, not for public repo) ---
CLAUDE.md
